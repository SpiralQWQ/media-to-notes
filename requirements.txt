# —— 图像文字识别 OCR（PaddleOCR 优先，RapidOCR 兜底）——
paddleocr>=2.7
paddlepaddle>=2.6
rapidocr-onnxruntime>=1.3

# —— 视频抽帧 / 场景切换检测 ——
opencv-python>=4.8

# —— 语音转写 ASR（FunASR / SenseVoiceSmall，体积大，建议独立 venv）——
funasr>=1.2
modelscope>=1.14
soundfile>=0.12

# —— 配置加载（可选，允许从 .env 读取环境变量）——
python-dotenv>=1.0

# —— 下载器（按来源网站）——
# 抖音：jiji262/douyin-downloader（需 Cookie，见 README「Cookie 设置」；非 pip 包，需克隆源码）
# YouTube/B站等：yt-dlp>=2024.0（可选，仅当使用抖音以外链接时）

# —— 系统依赖（需手动安装，加入 PATH）——
# ffmpeg / ffprobe
